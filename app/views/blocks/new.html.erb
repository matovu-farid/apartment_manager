<div class=" w-[60%] p-5 grid place-items-center">
  <div class='w-full'>
    <div>
      <h1 class="font-bold text-4xl ">New block</h1>
      <%= render "form", block: @block %>
    </div>
  </div>

  <div class='w-full'>
  <p class='font-bold text-xl'>OR Pull block</p>
  <p class='italic text-secondary text-sm'>Pulling a block means becoming an admin for a block created by another user. You need to enter the key the from block admin</p>
  <div>
    <%= form_with url: block_pull_url	 do |form| %>
      <div class="">
        <%= form.label :key, class: "form-label" %>
        <%= form.text_field :key, class: "form-control" %>
      </div>
      <div class="py-3">
        <%= form.submit class: "btn btn-outline-dark" %>
      </div>
    <% end %>
  </div>
  </div>
</div>
