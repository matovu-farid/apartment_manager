<div class="w-full p-3">
    <% if notice.present? %>
    <p class="
        py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block
      " id="notice"><%= notice %></p>
    <% end %>
    <% content_for :title, "Blocks" %>
    <%= render "block_header" %>
    <%= render "block_table", blocks: @blocks %>
</div>